{
  "name": "haehnchen-print-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Lokaler Windows-Print-Agent für das Hähnchen-Bestellsystem",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file=.env dist/index.js",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test dist/printer.spec.js"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
